<h2 class="mt-4">Trending</h2>

<% @polls.each do |poll| %>
  <%= render partial: "polls/poll",
              locals: { poll: poll,
              show_photo: true,
              show_buttons: true,
              show_my_poll: true } %>
<% end %>

